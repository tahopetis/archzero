# Arc Zero Production Environment Configuration
# Copy this file to .env and update with production values

# ============================================================================
# DATABASE CONFIGURATION
# ============================================================================

# PostgreSQL
POSTGRES_PASSWORD=your-strong-postgres-password-here
POSTGRES_PORT=5432

# Neo4j
NEO4J_PASSWORD=your-strong-neo4j-password-here
NEO4J_HTTP_PORT=7474
NEO4J_BOLT_PORT=7687

# Redis
REDIS_PASSWORD=your-strong-redis-password-here
REDIS_PORT=6379

# ============================================================================
# APPLICATION CONFIGURATION
# ============================================================================

# JWT Secret - Generate with: openssl rand -base64 32
JWT_SECRET=change-this-to-a-strong-random-secret-key
JWT_EXPIRATION_HOURS=24

# Server Ports
API_PORT=8080
UI_PORT=80

# CORS - Comma-separated list of allowed origins
CORS_ALLOWED_ORIGINS=https://your-domain.com,https://www.your-domain.com

# ============================================================================
# LOGGING
# ============================================================================

# Rust log level: error, warn, info, debug, trace
RUST_LOG=info

# ============================================================================
# SECURITY NOTES
# ============================================================================

# IMPORTANT:
# 1. Change all default passwords
# 2. Use strong, unique passwords for each service
# 3. Generate JWT_SECRET with: openssl rand -base64 32
# 4. Update CORS_ALLOWED_ORIGINS with your actual domain
# 5. Store this file securely and never commit to git
# 6. Use secrets management in production (HashiCorp Vault, AWS Secrets Manager, etc.)
